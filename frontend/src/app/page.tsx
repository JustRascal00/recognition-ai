import Face from "../app/Face/Face";  // Adjust the import path to point to the Face component

const Home = () => {
    return (
        <div>
            <Face />
        </div>
    );
};

export default Home;
